<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>ELEGAN'ZA — Pole Dance Studio</title>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet"/>
<style>
*{margin:0;padding:0;box-sizing:border-box}
:root{
  --gold:#C9A96E;--gold-light:#E8D5A3;--gold-dark:#8B6914;
  --black:#0A0A0A;--dark:#1A1A1A;--mid:#2A2A2A;--gray:#3A3A3A;
  --light-gray:#888;--white:#FDFAF5;--danger:#B84040;--success:#4A8C6A;
  --font-display:'Cormorant Garamond',serif;--font-body:'Montserrat',sans-serif;
  --radius:4px;--radius-lg:8px;
}
body{font-family:var(--font-body);background:var(--black);color:var(--white);min-height:100vh;overflow-x:hidden}

/* NAV */
nav{position:fixed;top:0;left:0;right:0;z-index:100;background:rgba(10,10,10,0.95);backdrop-filter:blur(12px);border-bottom:1px solid rgba(201,169,110,0.15);padding:0 2rem}
.nav-inner{max-width:1280px;margin:0 auto;display:flex;align-items:center;justify-content:space-between;height:64px}
.logo{font-family:var(--font-display);font-size:1.6rem;font-weight:300;letter-spacing:0.12em;color:var(--gold);font-style:italic}
.logo span{font-style:normal;font-weight:600}
.nav-links{display:flex;gap:1.5rem;align-items:center}
.nav-links button{background:none;border:none;color:var(--light-gray);font-family:var(--font-body);font-size:0.72rem;font-weight:500;letter-spacing:0.12em;text-transform:uppercase;cursor:pointer;padding:6px 0;transition:color 0.2s;border-bottom:1px solid transparent}
.nav-links button:hover,.nav-links button.active{color:var(--gold);border-bottom-color:var(--gold)}
.nav-btn-admin{background:var(--gold)!important;color:var(--black)!important;padding:6px 16px!important;border-radius:var(--radius)!important;border:none!important}
.nav-btn-admin:hover{background:var(--gold-light)!important}

/* HERO */
.hero{min-height:100vh;display:flex;align-items:center;justify-content:center;position:relative;overflow:hidden;background:radial-gradient(ellipse at 60% 40%,rgba(201,169,110,0.06) 0%,transparent 60%),linear-gradient(160deg,#0A0A0A 0%,#141414 50%,#0A0A0A 100%)}
.hero-bg{position:absolute;inset:0;opacity:0.04;background-image:repeating-linear-gradient(45deg,var(--gold) 0,var(--gold) 1px,transparent 0,transparent 50%);background-size:30px 30px}
.hero-content{text-align:center;z-index:1;padding:0 2rem}
.hero-eyebrow{font-size:0.7rem;letter-spacing:0.3em;text-transform:uppercase;color:var(--gold);margin-bottom:1.5rem;font-weight:500}
.hero-title{font-family:var(--font-display);font-size:clamp(3.5rem,8vw,7rem);font-weight:300;line-height:1.05;letter-spacing:0.04em;margin-bottom:0.3rem}
.hero-title em{font-style:italic;color:var(--gold)}
.hero-subtitle{font-family:var(--font-display);font-size:clamp(1.1rem,2.5vw,1.8rem);font-weight:300;color:var(--light-gray);margin-bottom:2.5rem;letter-spacing:0.08em}
.hero-divider{width:80px;height:1px;background:linear-gradient(90deg,transparent,var(--gold),transparent);margin:0 auto 2.5rem}
.hero-desc{font-size:0.82rem;letter-spacing:0.1em;color:var(--light-gray);max-width:420px;margin:0 auto 3rem;line-height:1.8;font-weight:300}
.btn-primary{background:var(--gold);color:var(--black);border:none;padding:14px 40px;font-family:var(--font-body);font-size:0.72rem;font-weight:600;letter-spacing:0.16em;text-transform:uppercase;cursor:pointer;border-radius:var(--radius);transition:all 0.25s}
.btn-primary:hover{background:var(--gold-light);transform:translateY(-1px)}
.btn-outline{background:transparent;color:var(--gold);border:1px solid var(--gold);padding:13px 40px;font-family:var(--font-body);font-size:0.72rem;font-weight:600;letter-spacing:0.16em;text-transform:uppercase;cursor:pointer;border-radius:var(--radius);transition:all 0.25s}
.btn-outline:hover{background:rgba(201,169,110,0.08)}
.hero-btns{display:flex;gap:1rem;justify-content:center;flex-wrap:wrap}

/* PAGES */
.page{display:none;padding-top:80px;min-height:100vh;padding-bottom:4rem}
.page.active{display:block}
.container{max-width:1200px;margin:0 auto;padding:0 2rem}
.section-header{text-align:center;padding:4rem 0 3rem}
.section-eyebrow{font-size:0.68rem;letter-spacing:0.28em;text-transform:uppercase;color:var(--gold);margin-bottom:1rem;font-weight:500}
.section-title{font-family:var(--font-display);font-size:clamp(2rem,4vw,3.2rem);font-weight:300;letter-spacing:0.04em}
.section-title em{font-style:italic;color:var(--gold)}
.divider-thin{width:60px;height:1px;background:linear-gradient(90deg,transparent,var(--gold),transparent);margin:1.5rem auto}

/* INSTRUCTORAS */
.profesores-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:1.5rem;margin-bottom:2rem}
.profesor-card{background:var(--dark);border:1px solid rgba(201,169,110,0.15);border-radius:var(--radius-lg);padding:1.5rem;cursor:pointer;transition:all 0.25s}
.profesor-card:hover,.profesor-card.selected{border-color:var(--gold);background:rgba(201,169,110,0.06)}
.profesor-card.inactivo{opacity:0.4;cursor:not-allowed;filter:grayscale(1)}
.profesor-avatar{width:60px;height:60px;border-radius:50%;background:linear-gradient(135deg,var(--gold-dark),var(--gold));display:flex;align-items:center;justify-content:center;font-family:var(--font-display);font-size:1.4rem;font-weight:300;color:var(--black);margin-bottom:1rem}
.profesor-name{font-family:var(--font-display);font-size:1.05rem;font-weight:400;margin-bottom:0.3rem}
.profesor-spec{font-size:0.65rem;letter-spacing:0.1em;text-transform:uppercase;color:var(--gold);font-weight:500}
.profesor-rating{font-size:0.72rem;color:var(--light-gray);margin-top:0.4rem}

/* CALENDARIO */
.booking-layout{display:grid;grid-template-columns:1fr 370px;gap:2rem;align-items:start}
.calendar-panel{background:var(--dark);border:1px solid rgba(201,169,110,0.15);border-radius:var(--radius-lg);padding:1.5rem}
.cal-header{display:flex;align-items:center;justify-content:space-between;margin-bottom:1.5rem}
.cal-month{font-family:var(--font-display);font-size:1.3rem;font-weight:400;letter-spacing:0.06em}
.cal-nav{background:none;border:1px solid rgba(201,169,110,0.3);color:var(--gold);width:32px;height:32px;border-radius:var(--radius);cursor:pointer;font-size:1rem;transition:all 0.2s}
.cal-nav:hover{background:rgba(201,169,110,0.1)}
.cal-grid{display:grid;grid-template-columns:repeat(7,1fr);gap:4px}
.cal-day-name{text-align:center;font-size:0.6rem;letter-spacing:0.08em;text-transform:uppercase;color:var(--light-gray);padding:6px 0;font-weight:500}
.cal-day{text-align:center;padding:8px 4px;border-radius:var(--radius);font-size:0.78rem;transition:all 0.2s;min-height:36px;display:flex;align-items:center;justify-content:center;flex-direction:column;gap:2px}
.cal-day.empty,.cal-day.past{color:var(--gray);cursor:default}
.cal-day.has-slots{color:var(--white);cursor:pointer}
.cal-day.has-slots::after{content:'';width:4px;height:4px;border-radius:50%;background:var(--gold);display:block}
.cal-day.selected{background:var(--gold);color:var(--black)!important;font-weight:600;cursor:pointer}
.cal-day.selected::after{background:var(--black)}
.cal-day.has-slots:hover:not(.selected){background:rgba(201,169,110,0.12)}

/* HORARIOS */
.slots-panel{background:var(--dark);border:1px solid rgba(201,169,110,0.15);border-radius:var(--radius-lg);padding:1.5rem}
.slots-title{font-family:var(--font-display);font-size:1.05rem;font-weight:400;margin-bottom:1rem;color:var(--gold)}
.slot-item{border:1px solid rgba(201,169,110,0.15);border-radius:var(--radius);padding:12px 14px;margin-bottom:8px;cursor:pointer;transition:all 0.2s;display:flex;align-items:center;justify-content:space-between}
.slot-item:hover:not(.booked){border-color:var(--gold);background:rgba(201,169,110,0.05)}
.slot-item.selected{border-color:var(--gold);background:rgba(201,169,110,0.1)}
.slot-item.booked{opacity:0.4;cursor:not-allowed;border-style:dashed}
.slot-time{font-weight:600;font-size:0.82rem;letter-spacing:0.06em}
.slot-info{font-size:0.68rem;color:var(--light-gray);margin-top:2px}
.slot-price{font-family:var(--font-display);font-size:1rem;color:var(--gold)}
.slot-badge{font-size:0.6rem;padding:3px 8px;border-radius:2px;letter-spacing:0.08em;text-transform:uppercase;font-weight:600}
.badge-available{background:rgba(74,140,106,0.15);color:#6DBFA0;border:1px solid rgba(74,140,106,0.3)}
.badge-full{background:rgba(184,64,64,0.15);color:#D98080;border:1px solid rgba(184,64,64,0.3)}

/* FORMULARIO RESERVA */
.booking-form{background:var(--dark);border:1px solid rgba(201,169,110,0.2);border-radius:var(--radius-lg);padding:1.5rem;margin-top:1.2rem}
.form-title{font-family:var(--font-display);font-size:1.15rem;font-weight:400;margin-bottom:1.2rem;color:var(--gold)}
.form-group{margin-bottom:1rem}
.form-label{font-size:0.65rem;letter-spacing:0.12em;text-transform:uppercase;color:var(--light-gray);font-weight:500;display:block;margin-bottom:6px}
.form-input{width:100%;background:var(--mid);border:1px solid rgba(201,169,110,0.2);border-radius:var(--radius);padding:10px 12px;color:var(--white);font-family:var(--font-body);font-size:0.82rem;transition:border-color 0.2s;outline:none}
.form-input:focus{border-color:var(--gold)}
.form-input::placeholder{color:var(--gray)}
select.form-input option{background:var(--dark)}
.booking-summary{background:rgba(201,169,110,0.06);border:1px solid rgba(201,169,110,0.15);border-radius:var(--radius);padding:1rem;margin:1rem 0}
.summary-row{display:flex;justify-content:space-between;font-size:0.78rem;margin-bottom:6px}
.summary-row.total{border-top:1px solid rgba(201,169,110,0.15);padding-top:8px;margin-top:4px;font-family:var(--font-display);font-size:0.95rem;font-weight:600}
.summary-label{color:var(--light-gray)}
.summary-value.gold{color:var(--gold)}

/* MIS CLASES */
.mis-clases-grid{display:grid;gap:1rem}
.clase-card{background:var(--dark);border:1px solid rgba(201,169,110,0.12);border-radius:var(--radius-lg);padding:1.2rem 1.5rem;display:flex;align-items:center;justify-content:space-between;gap:1rem;flex-wrap:wrap}
.clase-info{flex:1;min-width:200px}
.clase-curso{font-family:var(--font-display);font-size:1rem;font-weight:400;margin-bottom:0.25rem}
.clase-meta{font-size:0.72rem;color:var(--light-gray);display:flex;gap:1rem;flex-wrap:wrap;margin-top:4px}
/* FIX BUG 1: clases status en español */
.clase-status{font-size:0.65rem;padding:4px 10px;border-radius:2px;letter-spacing:0.08em;text-transform:uppercase;font-weight:600;white-space:nowrap}
.status-confirmada{background:rgba(74,140,106,0.15);color:#6DBFA0;border:1px solid rgba(74,140,106,0.3)}
.status-cancelada{background:rgba(184,64,64,0.15);color:#D98080;border:1px solid rgba(184,64,64,0.3)}
.btn-cancel{background:none;border:1px solid rgba(184,64,64,0.3);color:#D98080;padding:7px 14px;border-radius:var(--radius);font-size:0.68rem;letter-spacing:0.1em;text-transform:uppercase;cursor:pointer;font-family:var(--font-body);font-weight:600;transition:all 0.2s;white-space:nowrap}
.btn-cancel:hover{background:rgba(184,64,64,0.1);border-color:#D98080}
.btn-sm{padding:8px 20px;font-size:0.68rem}
.btn-icon{background:none;border:1px solid rgba(201,169,110,0.25);color:var(--gold);width:30px;height:30px;border-radius:var(--radius);cursor:pointer;font-size:0.85rem;transition:all 0.2s;display:inline-flex;align-items:center;justify-content:center}
.btn-icon:hover{background:rgba(201,169,110,0.1)}
.btn-icon.danger{border-color:rgba(184,64,64,0.3);color:#D98080}
.btn-icon.danger:hover{background:rgba(184,64,64,0.1)}

/* NOTIFICACIONES */
.notif-container{position:fixed;top:76px;right:1.5rem;z-index:500;display:flex;flex-direction:column;gap:10px;pointer-events:none;width:340px}
.notif{background:var(--mid);border:1px solid rgba(201,169,110,0.2);border-radius:var(--radius-lg);padding:14px 16px;display:flex;gap:12px;align-items:flex-start;animation:slideIn 0.3s ease;pointer-events:all;box-shadow:0 8px 32px rgba(0,0,0,0.6)}
.notif.success{border-color:rgba(74,140,106,0.4);background:rgba(20,35,22,0.97)}
.notif.warning{border-color:rgba(184,64,64,0.4);background:rgba(35,15,15,0.97)}
.notif-icon{width:28px;height:28px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:0.9rem;flex-shrink:0}
.notif.success .notif-icon{background:rgba(74,140,106,0.2);color:#6DBFA0}
.notif.warning .notif-icon{background:rgba(184,64,64,0.2);color:#D98080}
.notif-title{font-size:0.78rem;font-weight:600;margin-bottom:3px}
.notif.success .notif-title{color:#6DBFA0}
.notif.warning .notif-title{color:#D98080}
.notif-msg{font-size:0.72rem;color:var(--light-gray);line-height:1.5;flex:1}
.notif-close{background:none;border:none;color:var(--gray);cursor:pointer;font-size:1.1rem;flex-shrink:0;line-height:1}
@keyframes slideIn{from{transform:translateX(110%);opacity:0}to{transform:translateX(0);opacity:1}}
@keyframes fadeOut{from{opacity:1}to{opacity:0;transform:translateX(110%)}}
.notif.closing{animation:fadeOut 0.3s ease forwards}

/* MODAL */
.overlay{display:none;position:fixed;inset:0;background:rgba(0,0,0,0.75);z-index:300;align-items:center;justify-content:center}
.overlay.open{display:flex}
.modal{background:var(--dark);border:1px solid rgba(201,169,110,0.25);border-radius:var(--radius-lg);padding:2rem;max-width:480px;width:92%;max-height:90vh;overflow-y:auto}
.modal h3{font-family:var(--font-display);font-size:1.5rem;font-weight:300;margin-bottom:0.75rem}
.modal p{font-size:0.78rem;color:var(--light-gray);line-height:1.7;margin-bottom:1.5rem}
.modal-btns{display:flex;gap:0.75rem;justify-content:flex-end;margin-top:1.5rem;flex-wrap:wrap}
.btn-danger{background:var(--danger);color:var(--white);border:none;padding:10px 24px;border-radius:var(--radius);font-family:var(--font-body);font-size:0.72rem;font-weight:600;letter-spacing:0.1em;text-transform:uppercase;cursor:pointer;transition:all 0.2s}
.btn-danger:hover{background:#C04444}

/* ADMIN */
.admin-lock{text-align:center;padding:4rem 2rem;max-width:360px;margin:0 auto}
.admin-lock-icon{font-size:2.5rem;margin-bottom:1.5rem;opacity:0.35}
.admin-lock h2{font-family:var(--font-display);font-size:1.8rem;font-weight:300;margin-bottom:0.75rem}
.admin-lock p{font-size:0.78rem;color:var(--light-gray);margin-bottom:2rem;line-height:1.7}
.tab-bar{display:flex;border:1px solid rgba(201,169,110,0.15);border-radius:var(--radius);overflow:hidden;margin-bottom:2rem}
.tab{flex:1;padding:10px 6px;background:none;border:none;color:var(--light-gray);font-family:var(--font-body);font-size:0.68rem;font-weight:600;letter-spacing:0.1em;text-transform:uppercase;cursor:pointer;transition:all 0.2s}
.tab.active{background:rgba(201,169,110,0.1);color:var(--gold)}
.tab:hover:not(.active){background:rgba(255,255,255,0.03)}
.metrics-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(190px,1fr));gap:1rem;margin-bottom:2rem}
.metric-card{background:var(--dark);border:1px solid rgba(201,169,110,0.12);border-radius:var(--radius-lg);padding:1.4rem}
.metric-label{font-size:0.62rem;letter-spacing:0.14em;text-transform:uppercase;color:var(--light-gray);margin-bottom:0.6rem;font-weight:500}
.metric-value{font-family:var(--font-display);font-size:1.9rem;font-weight:300;line-height:1}
.metric-sub{font-size:0.68rem;color:var(--light-gray);margin-top:0.4rem}
.metric-value.gold{color:var(--gold)}
.metric-value.green{color:#6DBFA0}
.metric-value.red{color:#D98080}
.chart-panel{background:var(--dark);border:1px solid rgba(201,169,110,0.12);border-radius:var(--radius-lg);padding:1.5rem;margin-bottom:2rem}
.chart-title{font-family:var(--font-display);font-size:1rem;color:var(--gold);margin-bottom:1.2rem}
.legend{display:flex;gap:1.5rem;margin-bottom:1rem}
.legend-item{display:flex;align-items:center;gap:6px;font-size:0.66rem;color:var(--light-gray);text-transform:uppercase;letter-spacing:0.08em}
.legend-dot{width:10px;height:10px;border-radius:2px}
.bar-chart{display:flex;gap:8px;align-items:flex-end;height:130px}
.bar-group{flex:1;display:flex;flex-direction:column;align-items:center;gap:4px}
.bar-wrap{flex:1;width:100%;display:flex;flex-direction:column;justify-content:flex-end;gap:2px}
.bar{border-radius:3px 3px 0 0;width:100%;min-height:3px;transition:height 0.4s}
.bar.income{background:linear-gradient(180deg,var(--gold),var(--gold-dark))}
.bar.cost{background:rgba(184,64,64,0.5)}
.bar-label{font-size:0.58rem;color:var(--light-gray);text-transform:uppercase}
.data-table{background:var(--dark);border:1px solid rgba(201,169,110,0.12);border-radius:var(--radius-lg);overflow:hidden;margin-bottom:1.5rem}
.data-table-header{background:rgba(201,169,110,0.06);padding:1rem 1.5rem;border-bottom:1px solid rgba(201,169,110,0.1);display:flex;align-items:center;justify-content:space-between}
.data-table-header h3{font-family:var(--font-display);font-size:1rem;color:var(--gold)}
table{width:100%;border-collapse:collapse}
th{font-size:0.6rem;letter-spacing:0.12em;text-transform:uppercase;color:var(--light-gray);font-weight:600;padding:10px 1.2rem;text-align:left;border-bottom:1px solid rgba(201,169,110,0.08)}
td{font-size:0.76rem;padding:11px 1.2rem;border-bottom:1px solid rgba(201,169,110,0.05)}
tr:last-child td{border-bottom:none}
tr:hover td{background:rgba(201,169,110,0.02)}
.td-pos{color:#6DBFA0}
.td-neg{color:#D98080}
.empty-state{text-align:center;padding:3rem 1rem;color:var(--light-gray)}
.empty-state .empty-icon{font-size:2.5rem;opacity:0.25;margin-bottom:1rem}
.empty-state p{font-family:var(--font-display);font-size:1.3rem;color:var(--gray)}

/* ADMIN EDIT */
.admin-section{background:var(--dark);border:1px solid rgba(201,169,110,0.12);border-radius:var(--radius-lg);padding:1.5rem;margin-bottom:1.5rem}
.admin-section-title{font-family:var(--font-display);font-size:1.1rem;color:var(--gold);margin-bottom:1.2rem;display:flex;align-items:center;justify-content:space-between}
.edit-row{display:grid;grid-template-columns:1fr 1fr;gap:1rem;margin-bottom:1rem}
.edit-row-3{display:grid;grid-template-columns:1fr 1fr 1fr;gap:1rem;margin-bottom:1rem}
.toggle-switch{display:flex;align-items:center;gap:8px;cursor:pointer;user-select:none}
.toggle-switch input{display:none}
.toggle-track{width:40px;height:22px;background:var(--gray);border-radius:11px;position:relative;transition:background 0.2s}
.toggle-track::after{content:'';width:16px;height:16px;background:#fff;border-radius:50%;position:absolute;top:3px;left:3px;transition:transform 0.2s}
.toggle-switch input:checked+.toggle-track{background:var(--gold)}
.toggle-switch input:checked+.toggle-track::after{transform:translateX(18px)}
.horario-tag{display:inline-flex;align-items:center;gap:4px;background:rgba(201,169,110,0.1);border:1px solid rgba(201,169,110,0.25);border-radius:var(--radius);padding:4px 10px;font-size:0.7rem;color:var(--gold);margin:3px}
.horario-tag button{background:none;border:none;color:var(--gold);cursor:pointer;font-size:0.8rem;line-height:1;padding:0 2px}
.horario-tag button:hover{color:#D98080}
.profesor-admin-card{background:rgba(201,169,110,0.04);border:1px solid rgba(201,169,110,0.12);border-radius:var(--radius-lg);padding:1.2rem;margin-bottom:1rem}
.profesor-admin-card .card-top{display:flex;align-items:flex-start;justify-content:space-between;margin-bottom:1rem}

@media(max-width:800px){
  .booking-layout{grid-template-columns:1fr}
  .edit-row,.edit-row-3{grid-template-columns:1fr}
  .nav-links{gap:0.8rem}
  .nav-links button{font-size:0.6rem}
}
</style>
</head>
<body>

<nav>
  <div class="nav-inner">
    <div class="logo">Elegan<span>'za</span></div>
    <div class="nav-links">
      <button class="active" onclick="showPage('home',this)">Inicio</button>
      <button onclick="showPage('clases',this)">Clases</button>
      <button onclick="showPage('reservas',this)">Reservar</button>
      <button onclick="showPage('mis-clases',this)">Mis Clases</button>
      <button class="nav-btn-admin" onclick="showPage('admin',this)">Admin</button>
    </div>
  </div>
</nav>

<div class="notif-container" id="notifContainer"></div>

<!-- MODAL CANCELAR -->
<div class="overlay" id="cancelModal">
  <div class="modal">
    <h3>Cancelar Clase</h3>
    <p>¿Confirmas la cancelación? Se notificará al estudiante inmediatamente.</p>
    <div class="modal-btns">
      <button class="btn-outline btn-sm" onclick="closeCancelModal()">Volver</button>
      <button class="btn-danger" onclick="confirmCancel()">Sí, cancelar</button>
    </div>
  </div>
</div>

<!-- MODAL EDITAR PROFESOR -->
<div class="overlay" id="modalProfesor">
  <div class="modal">
    <h3 id="modalProfTitle">Nuevo Profesor</h3>
    <div class="form-group">
      <label class="form-label">Nombre completo</label>
      <input class="form-input" id="mpNombre" placeholder="Ej: Valentina Rojas"/>
    </div>
    <div class="form-group">
      <label class="form-label">Especialidad</label>
      <input class="form-input" id="mpSpec" placeholder="Ej: Pole Art & Performance"/>
    </div>
    <div class="form-group">
      <label class="form-label">Iniciales (máx 2)</label>
      <input class="form-input" id="mpIniciales" placeholder="Ej: VR" maxlength="2"/>
    </div>
    <div class="form-group">
      <label class="form-label">Cursos que imparte</label>
      <div id="mpCursosCheck"></div>
    </div>
    <div class="form-group">
      <label class="form-label">Horarios disponibles</label>
      <div id="mpHorariosCheck"></div>
    </div>
    <label class="toggle-switch" style="margin-top:0.5rem">
      <input type="checkbox" id="mpActivo" checked/>
      <div class="toggle-track"></div>
      <span style="font-size:0.75rem;color:var(--light-gray)">Profesor activo</span>
    </label>
    <div class="modal-btns">
      <button class="btn-outline btn-sm" onclick="closeModal('modalProfesor')">Cancelar</button>
      <button class="btn-primary btn-sm" onclick="guardarProfesor()">Guardar</button>
    </div>
  </div>
</div>

<!-- MODAL EDITAR CURSO -->
<div class="overlay" id="modalCurso">
  <div class="modal">
    <h3 id="modalCursoTitle">Editar Curso</h3>
    <div class="form-group">
      <label class="form-label">Nombre del curso</label>
      <input class="form-input" id="mcNombre" placeholder="Ej: Pole Flow"/>
    </div>
    <div class="form-group">
      <label class="form-label">Nivel</label>
      <select class="form-input" id="mcNivel">
        <option>Principiante</option><option>Intermedio</option><option>Avanzado</option>
      </select>
    </div>
    <div class="edit-row">
      <div class="form-group" style="margin:0">
        <label class="form-label">Precio por clase ($)</label>
        <input class="form-input" id="mcPrecio" type="number" placeholder="18000"/>
      </div>
      <div class="form-group" style="margin:0">
        <label class="form-label">Costo operativo ($)</label>
        <input class="form-input" id="mcCosto" type="number" placeholder="8000"/>
      </div>
    </div>
    <div class="modal-btns">
      <button class="btn-outline btn-sm" onclick="closeModal('modalCurso')">Cancelar</button>
      <button class="btn-primary btn-sm" onclick="guardarCurso()">Guardar</button>
    </div>
  </div>
</div>

<!-- =================== PÁGINAS =================== -->

<!-- INICIO -->
<div id="page-home" class="page active">
  <section class="hero">
    <div class="hero-bg"></div>
    <div class="hero-content">
      <div class="hero-eyebrow">Studio Premium · Santiago, Chile</div>
      <h1 class="hero-title">Elegan<em>'za</em><br>Pole Dance</h1>
      <p class="hero-subtitle">Arte · Fuerza · Elegancia</p>
      <div class="hero-divider"></div>
      <p class="hero-desc">Descubre el poder de tu cuerpo a través del pole dance. Clases personalizadas con instructoras certificadas para todos los niveles.</p>
      <div class="hero-btns">
        <button class="btn-primary" onclick="showPage('reservas',document.querySelectorAll('.nav-links button')[2])">Reservar Clase</button>
        <button class="btn-outline" onclick="showPage('clases',document.querySelectorAll('.nav-links button')[1])">Ver Programas</button>
      </div>
    </div>
  </section>
</div>

<!-- CLASES -->
<div id="page-clases" class="page">
  <div class="container">
    <div class="section-header">
      <div class="section-eyebrow">Nuestros Programas</div>
      <h2 class="section-title">Clases <em>diseñadas</em> para ti</h2>
      <div class="divider-thin"></div>
    </div>
    <div class="profesores-grid" id="profesoresGridClases"></div>
    <div id="cursosGridClases" style="display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:1.5rem;margin-top:1rem"></div>
  </div>
</div>

<!-- RESERVAR -->
<div id="page-reservas" class="page">
  <div class="container">
    <div class="section-header">
      <div class="section-eyebrow">Sistema de Reservas</div>
      <h2 class="section-title">Elige tu <em>clase perfecta</em></h2>
      <div class="divider-thin"></div>
    </div>
    <div style="margin-bottom:2rem">
      <div style="font-size:0.66rem;letter-spacing:0.2em;text-transform:uppercase;color:var(--gold);margin-bottom:1rem;font-weight:600">Paso 1 — Elige tu instructora</div>
      <div class="profesores-grid" id="profesoresGridReserva"></div>
    </div>
    <div id="bookingStep2" style="display:none">
      <div style="font-size:0.66rem;letter-spacing:0.2em;text-transform:uppercase;color:var(--gold);margin-bottom:1rem;font-weight:600">Paso 2 — Selecciona fecha y hora disponible</div>
      <div class="booking-layout">
        <div>
          <div class="calendar-panel">
            <div class="cal-header">
              <button class="cal-nav" onclick="changeMonth(-1)">&#8249;</button>
              <span class="cal-month" id="calMonthLabel"></span>
              <button class="cal-nav" onclick="changeMonth(1)">&#8250;</button>
            </div>
            <div class="cal-grid" id="calGrid"></div>
          </div>
        </div>
        <div>
          <div class="slots-panel">
            <div class="slots-title" id="slotsTitle">Selecciona un día</div>
            <div id="slotsList">
              <div class="empty-state"><p style="font-size:0.95rem;margin-top:1rem">↑ Elige una fecha disponible</p></div>
            </div>
          </div>
          <div class="booking-form" id="bookingForm" style="display:none">
            <div class="form-title">Paso 3 — Completa tu reserva</div>
            <div class="form-group">
              <label class="form-label">Nombre completo</label>
              <input class="form-input" id="inputNombre" placeholder="Ej: María González"/>
            </div>
            <div class="form-group">
              <label class="form-label">Correo electrónico</label>
              <input class="form-input" type="email" id="inputEmail" placeholder="tu@correo.cl"/>
            </div>
            <div class="form-group">
              <label class="form-label">Teléfono</label>
              <input class="form-input" type="tel" id="inputTel" placeholder="+56 9 xxxx xxxx"/>
            </div>
            <div class="booking-summary" id="bookingSummary"></div>
            <button class="btn-primary" style="width:100%;margin-top:0.5rem" onclick="confirmarReserva()">✓ Confirmar Reserva</button>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- MIS CLASES -->
<div id="page-mis-clases" class="page">
  <div class="container">
    <div class="section-header">
      <div class="section-eyebrow">Tu agenda</div>
      <h2 class="section-title">Mis <em>Clases</em></h2>
      <div class="divider-thin"></div>
    </div>
    <div class="mis-clases-grid" id="misClasesGrid"></div>
  </div>
</div>

<!-- ADMIN -->
<div id="page-admin" class="page">
  <div class="container">
    <div class="section-header">
      <div class="section-eyebrow">Panel Administrativo</div>
      <h2 class="section-title">Gestión <em>Elegan'za</em></h2>
      <div class="divider-thin"></div>
    </div>
    <div id="adminLock">
      <div class="admin-lock">
        <div class="admin-lock-icon">&#128272;</div>
        <h2>Acceso Restringido</h2>
        <p>Solo para el equipo de Elegan'za.</p>
        <div class="form-group">
          <input class="form-input" type="password" id="adminPass" placeholder="Contraseña"
            onkeydown="if(event.key==='Enter')checkAdmin()"/>
        </div>
        <button class="btn-primary" style="width:100%;margin-top:0.5rem" onclick="checkAdmin()">Ingresar</button>
        <p style="font-size:0.68rem;color:var(--gray);margin-top:1rem">Demo: "admin123"</p>
      </div>
    </div>
    <div id="adminDash" style="display:none">
      <div class="tab-bar">
        <button class="tab active" onclick="adminTab('resumen',this)">Resumen</button>
        <button class="tab" onclick="adminTab('profesores',this)">Profesores</button>
        <button class="tab" onclick="adminTab('cursos',this)">Cursos</button>
        <button class="tab" onclick="adminTab('reservas',this)">Reservas</button>
      </div>
      <!-- RESUMEN -->
      <div id="tabResumen">
        <div class="metrics-grid">
          <div class="metric-card"><div class="metric-label">Ingresos del mes</div><div class="metric-value gold" id="metIngresos">$0</div><div class="metric-sub" id="metIngSub">—</div></div>
          <div class="metric-card"><div class="metric-label">Costos operativos</div><div class="metric-value red" id="metCostos">$0</div><div class="metric-sub">Arriendo + salarios</div></div>
          <div class="metric-card"><div class="metric-label">Utilidad neta</div><div class="metric-value" id="metUtilidad">$0</div><div class="metric-sub" id="metUtiSub">—</div></div>
          <div class="metric-card"><div class="metric-label">Reservas activas</div><div class="metric-value" id="metReservas">0</div><div class="metric-sub">este mes</div></div>
        </div>
        <div class="chart-panel">
          <div class="chart-title">Ingresos vs Costos — Últimos 6 meses</div>
          <div class="legend">
            <div class="legend-item"><div class="legend-dot" style="background:var(--gold)"></div>Ingresos</div>
            <div class="legend-item"><div class="legend-dot" style="background:rgba(184,64,64,0.5)"></div>Costos</div>
          </div>
          <div class="bar-chart" id="barChart"></div>
        </div>
        <div style="text-align:right;margin-top:1rem">
          <button class="btn-danger btn-sm" onclick="simularCancelacionAdmin()">⚠ Simular Cancelación</button>
        </div>
      </div>
      <!-- PROFESORES -->
      <div id="tabProfesores" style="display:none">
        <div style="display:flex;justify-content:flex-end;margin-bottom:1rem">
          <button class="btn-primary btn-sm" onclick="abrirModalProfesor(null)">+ Agregar Instructor/a</button>
        </div>
        <div id="adminProfesoresList"></div>
      </div>
      <!-- CURSOS -->
      <div id="tabCursos" style="display:none">
        <div style="display:flex;justify-content:flex-end;margin-bottom:1rem">
          <button class="btn-primary btn-sm" onclick="abrirModalCurso(null)">+ Nuevo Curso</button>
        </div>
        <div id="adminCursosList"></div>
      </div>
      <!-- RESERVAS -->
      <div id="tabReservas" style="display:none">
        <div class="data-table">
          <div class="data-table-header"><h3>Historial de Reservas</h3></div>
          <table><thead><tr><th>Estudiante</th><th>Clase</th><th>Fecha</th><th>Instructora</th><th>Estado</th><th>Monto</th><th>Acción</th></tr></thead>
          <tbody id="reservasTableBody"></tbody></table>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
// =====================================================
// DATOS — se guardan en localStorage
// =====================================================
const TODOS_HORARIOS = ["08:00","09:30","11:00","14:00","15:30","17:00","18:30","20:00"];
const MESES = ["Enero","Febrero","Marzo","Abril","Mayo","Junio","Julio","Agosto","Septiembre","Octubre","Noviembre","Diciembre"];
const DIAS  = ["Do","Lu","Ma","Mi","Ju","Vi","Sá"];

function cargarDatos(){
  // Profesores por defecto
  const defProfs = [
    {id:1, nombre:"Benjamin Fuentes", spec:"Pole Art & Stripers",     inicial:"BF", activo:true, cursos:["Pole Intro","Pole Art"],  horarios:["09:30","11:00","15:30","17:00"]},
    {id:2, nombre:"Camila Torres",    spec:"Pole Flow & Exotic",       inicial:"CT", activo:true, cursos:["Pole Flow","Pole Intro"], horarios:["08:00","14:00","18:30","20:00"]},
    {id:3, nombre:"Daniela Muñoz",    spec:"Pole Fitness & Acrobático",inicial:"DM", activo:true, cursos:["Pole Flow","Pole Art"],  horarios:["09:30","14:00","17:00","18:30"]},
    {id:4, nombre:"Sofía Vega",       spec:"Pole Intro & Flexibility", inicial:"SV", activo:true, cursos:["Pole Intro"],            horarios:["08:00","11:00","15:30","20:00"]}
  ];
  const defCursos = [
    {id:1, nombre:"Pole Intro", nivel:"Principiante", precio:18000, costo:8000},
    {id:2, nombre:"Pole Flow",  nivel:"Intermedio",   precio:22000, costo:10000},
    {id:3, nombre:"Pole Art",   nivel:"Avanzado",     precio:26000, costo:12000}
  ];
  const defReservas = [
    {id:1, nombre:"María González",  email:"maria@gmail.com",    tel:"+56912345678", curso:"Pole Intro", precio:18000, hora:"09:30", fecha:"22 de mayo de 2026", profesora:"Benjamin Fuentes", estado:"confirmada"},
    {id:2, nombre:"Fernanda López",  email:"fernanda@gmail.com", tel:"+56987654321", curso:"Pole Flow",  precio:22000, hora:"14:00", fecha:"23 de mayo de 2026", profesora:"Camila Torres",    estado:"confirmada"},
    {id:3, nombre:"Daniela Pérez",   email:"daniela@gmail.com",  tel:"+56911111111", curso:"Pole Art",   precio:26000, hora:"17:00", fecha:"24 de mayo de 2026", profesora:"Daniela Muñoz",    estado:"cancelada"}
  ];
  return {
    profesores: JSON.parse(localStorage.getItem('ez_profesores')) || defProfs,
    cursos:     JSON.parse(localStorage.getItem('ez_cursos'))     || defCursos,
    reservas:   JSON.parse(localStorage.getItem('ez_reservas'))   || defReservas
  };
}

let { profesores, cursos, reservas } = cargarDatos();

function save(){
  localStorage.setItem('ez_profesores', JSON.stringify(profesores));
  localStorage.setItem('ez_cursos',     JSON.stringify(cursos));
  localStorage.setItem('ez_reservas',   JSON.stringify(reservas));
}
function fmt(n){ return n.toLocaleString('es-CL',{style:'currency',currency:'CLP',minimumFractionDigits:0}); }
function nextId(arr){ return arr.length ? Math.max(...arr.map(x=>x.id))+1 : 1; }

// =====================================================
// NAVEGACIÓN
// =====================================================
function showPage(id, btn){
  document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));
  document.getElementById('page-'+id).classList.add('active');
  document.querySelectorAll('.nav-links button').forEach(b=>b.classList.remove('active'));
  if(btn) btn.classList.add('active');
  if(id==='clases')     renderPaginaClases();
  if(id==='reservas')   initReservas();
  if(id==='mis-clases') renderMisClases();
}

// =====================================================
// PÁGINA CLASES
// =====================================================
function renderPaginaClases(){
  // Instructoras
  const gp = document.getElementById('profesoresGridClases');
  gp.innerHTML = profesores.filter(p=>p.activo).map(p=>`
    <div class="profesor-card">
      <div class="profesor-avatar">${p.inicial}</div>
      <div class="profesor-name">${p.nombre}</div>
      <div class="profesor-spec">${p.spec}</div>
      <div style="font-size:0.68rem;color:var(--light-gray);margin-top:0.5rem">${p.cursos.join(' · ')}</div>
    </div>`).join('');
  // Cursos
  const gc = document.getElementById('cursosGridClases');
  gc.innerHTML = cursos.map(c=>`
    <div style="background:var(--dark);border:1px solid rgba(201,169,110,0.12);border-radius:var(--radius-lg);padding:2rem">
      <div class="section-eyebrow" style="text-align:left;margin-bottom:0.6rem">${c.nivel}</div>
      <h3 style="font-family:var(--font-display);font-size:1.3rem;font-weight:300;margin-bottom:0.6rem">${c.nombre}</h3>
      <div style="font-family:var(--font-display);font-size:1.4rem;color:var(--gold);margin-bottom:1rem">${fmt(c.precio)} <span style="font-size:0.78rem;color:var(--light-gray)">/ clase</span></div>
      <button class="btn-primary btn-sm" onclick="showPage('reservas',document.querySelectorAll('.nav-links button')[2])">Reservar</button>
    </div>`).join('');
}

// =====================================================
// RESERVAS — CALENDARIO
// =====================================================
let calYear=2026, calMonth=4;
let selectedProfesor=null, selectedDate=null, selectedSlot=null;

function initReservas(){
  selectedProfesor=null; selectedDate=null; selectedSlot=null;
  document.getElementById('bookingStep2').style.display='none';
  renderProfesoresReserva();
  renderCalendar();
}

function renderProfesoresReserva(){
  const el = document.getElementById('profesoresGridReserva');
  const activos = profesores.filter(p=>p.activo);
  el.innerHTML = activos.map(p=>`
    <div class="profesor-card${selectedProfesor&&selectedProfesor.id===p.id?' selected':''}"
         onclick="selectProfesor(${p.id})">
      <div class="profesor-avatar">${p.inicial}</div>
      <div class="profesor-name">${p.nombre}</div>
      <div class="profesor-spec">${p.spec}</div>
      <div style="font-size:0.68rem;color:var(--light-gray);margin-top:0.4rem">${p.cursos.join(' · ')}</div>
    </div>`).join('');
}

function selectProfesor(id){
  selectedProfesor = profesores.find(p=>p.id===id);
  selectedDate = null; selectedSlot = null;
  renderProfesoresReserva();
  document.getElementById('bookingStep2').style.display = 'block';
  document.getElementById('bookingForm').style.display  = 'none';
  document.getElementById('slotsList').innerHTML = '<div class="empty-state"><p style="font-size:0.9rem;margin-top:0.5rem">↑ Elige una fecha disponible</p></div>';
  document.getElementById('slotsTitle').textContent = 'Selecciona un día';
  renderCalendar();
  document.getElementById('bookingStep2').scrollIntoView({behavior:'smooth', block:'start'});
}

function changeMonth(d){
  calMonth += d;
  if(calMonth>11){calMonth=0;calYear++;}
  if(calMonth<0){calMonth=11;calYear--;}
  renderCalendar();
}

function renderCalendar(){
  document.getElementById('calMonthLabel').textContent = `${MESES[calMonth]} ${calYear}`;
  const grid    = document.getElementById('calGrid');
  const today   = new Date(); today.setHours(0,0,0,0);
  const first   = new Date(calYear, calMonth, 1);
  const startDay= first.getDay();
  const daysIn  = new Date(calYear, calMonth+1, 0).getDate();

  let html = DIAS.map(d=>`<div class="cal-day-name">${d}</div>`).join('');
  for(let i=0;i<startDay;i++) html+=`<div class="cal-day empty"></div>`;

  for(let d=1;d<=daysIn;d++){
    const dt   = new Date(calYear, calMonth, d);
    const past = dt < today;
    // Un día tiene slots si el profesor tiene horarios configurados y no es pasado
    const hasSlots = !past && selectedProfesor && selectedProfesor.horarios && selectedProfesor.horarios.length > 0;
    const sel  = selectedDate && selectedDate.getDate()===d && selectedDate.getMonth()===calMonth && selectedDate.getFullYear()===calYear;
    let cls = 'cal-day';
    if(past) cls += ' past';
    else if(hasSlots) cls += ' has-slots';
    if(sel) cls += ' selected';
    const click = (hasSlots && !past) ? `onclick="selectDate(${d})"` : '';
    html += `<div class="${cls}" ${click}>${d}</div>`;
  }
  grid.innerHTML = html;
}

function selectDate(d){
  selectedDate = new Date(calYear, calMonth, d);
  selectedSlot = null;
  document.getElementById('bookingForm').style.display = 'none';
  renderCalendar();
  renderSlots();
}

// FIX BUG 2: slots filtrados por los horarios del profesor y cursos que imparte
function renderSlots(){
  const dateStr = selectedDate.toLocaleDateString('es-CL',{weekday:'long',year:'numeric',month:'long',day:'numeric'});
  document.getElementById('slotsTitle').textContent = dateStr;

  if(!selectedProfesor || !selectedProfesor.horarios || selectedProfesor.horarios.length===0){
    document.getElementById('slotsList').innerHTML = '<div class="empty-state"><p>Esta instructora no tiene horarios configurados.</p></div>';
    return;
  }

  // Horarios del profesor con su curso asignado (cíclico entre los cursos que imparte)
  const cursosProf = cursos.filter(c => selectedProfesor.cursos.includes(c.nombre));
  if(cursosProf.length===0){
    document.getElementById('slotsList').innerHTML = '<div class="empty-state"><p>Esta instructora no tiene cursos asignados.</p></div>';
    return;
  }

  // Verificar cuáles slots ya están reservados en esa fecha/profesor
  const fechaStr = selectedDate.toLocaleDateString('es-CL',{day:'numeric',month:'long',year:'numeric'});
  const reservasEnFecha = reservas.filter(r=>
    r.profesora === selectedProfesor.nombre &&
    r.fecha === fechaStr &&
    r.estado === 'confirmada'
  );
  const horasOcupadas = reservasEnFecha.map(r=>r.hora);

  const slots = selectedProfesor.horarios.map((h, i)=>{
    const curso  = cursosProf[i % cursosProf.length];
    const booked = horasOcupadas.includes(h);
    return { hora:h, curso:curso.nombre, precio:curso.precio, booked };
  });

  window._slots = slots;

  document.getElementById('slotsList').innerHTML = slots.map((s,i)=>`
    <div class="slot-item${s.booked?' booked':''}"
         ${!s.booked ? `onclick="selectSlot(${i})"` : ''}>
      <div>
        <div class="slot-time">${s.hora}</div>
        <div class="slot-info">${s.curso} · ${selectedProfesor.nombre.split(' ')[0]}</div>
      </div>
      <div style="display:flex;align-items:center;gap:8px">
        <div class="slot-price">${fmt(s.precio)}</div>
        <div class="slot-badge ${s.booked?'badge-full':'badge-available'}">${s.booked?'Reservado':'Disponible'}</div>
      </div>
    </div>`).join('');
}

function selectSlot(i){
  const slot = window._slots[i];
  selectedSlot = slot;
  document.querySelectorAll('#slotsList .slot-item').forEach((el,idx)=>{
    el.classList.toggle('selected', idx===i);
  });
  const dateStr = selectedDate.toLocaleDateString('es-CL',{day:'numeric',month:'long',year:'numeric'});
  document.getElementById('bookingSummary').innerHTML=`
    <div class="summary-row"><span class="summary-label">Clase</span><span class="summary-value">${slot.curso}</span></div>
    <div class="summary-row"><span class="summary-label">Instructora</span><span class="summary-value">${selectedProfesor.nombre}</span></div>
    <div class="summary-row"><span class="summary-label">Fecha</span><span class="summary-value">${dateStr}</span></div>
    <div class="summary-row"><span class="summary-label">Hora</span><span class="summary-value">${slot.hora}</span></div>
    <div class="summary-row total"><span class="summary-label">Total</span><span class="summary-value gold">${fmt(slot.precio)}</span></div>`;
  document.getElementById('bookingForm').style.display = 'block';
  document.getElementById('bookingForm').scrollIntoView({behavior:'smooth',block:'nearest'});
}

// FIX BUG 3: validación completa y guarda profesora correctamente
function confirmarReserva(){
  const nombre = document.getElementById('inputNombre').value.trim();
  const email  = document.getElementById('inputEmail').value.trim();
  const tel    = document.getElementById('inputTel').value.trim();
  if(!nombre || !email){ showNotif('warning','Campos requeridos','Por favor completa nombre y correo.'); return; }
  if(!selectedProfesor){ showNotif('warning','Falta instructora','Selecciona una instructora primero.'); return; }
  if(!selectedDate){     showNotif('warning','Falta fecha','Selecciona una fecha en el calendario.'); return; }
  if(!selectedSlot){     showNotif('warning','Falta horario','Selecciona un horario disponible.'); return; }

  const dateStr = selectedDate.toLocaleDateString('es-CL',{day:'numeric',month:'long',year:'numeric'});
  const nueva = {
    id: Date.now(),
    nombre, email, tel,
    curso:     selectedSlot.curso,
    precio:    selectedSlot.precio,
    hora:      selectedSlot.hora,
    fecha:     dateStr,
    profesora: selectedProfesor.nombre,   // ← guarda el nombre real del profesor seleccionado
    estado:    'confirmada'
  };
  reservas.unshift(nueva);
  save();
  showNotif('success','¡Reserva Confirmada! 🎉',`${nueva.curso} el ${nueva.fecha} a las ${nueva.hora} con ${selectedProfesor.nombre.split(' ')[0]}.`);
  document.getElementById('inputNombre').value='';
  document.getElementById('inputEmail').value='';
  document.getElementById('inputTel').value='';
  document.getElementById('bookingForm').style.display='none';
  selectedSlot=null;
  setTimeout(()=>showPage('mis-clases',document.querySelectorAll('.nav-links button')[3]),1300);
}

// =====================================================
// MIS CLASES
// =====================================================
function renderMisClases(){
  const el = document.getElementById('misClasesGrid');
  if(!reservas.length){
    el.innerHTML=`<div class="empty-state"><div class="empty-icon">📋</div><p>Aún no tienes clases reservadas</p>
      <button class="btn-outline btn-sm" style="margin-top:1.5rem"
        onclick="showPage('reservas',document.querySelectorAll('.nav-links button')[2])">Reservar mi primera clase</button></div>`;
    return;
  }
  el.innerHTML = reservas.map(r=>`
    <div class="clase-card">
      <div class="clase-info">
        <div class="clase-curso">${r.curso}</div>
        <div class="clase-meta">
          <span>📅 ${r.fecha}</span>
          <span>🕐 ${r.hora}</span>
          <span>👤 ${r.profesora ? r.profesora.split(' ')[0] : '—'}</span>
          <span style="color:var(--gold)">💰 ${fmt(r.precio)}</span>
        </div>
        <div style="font-size:0.68rem;color:var(--light-gray);margin-top:4px">${r.nombre} · ${r.email}</div>
      </div>
      <div style="display:flex;align-items:center;gap:0.75rem">
        <span class="clase-status status-${r.estado}">${r.estado==='confirmada'?'Confirmada':'Cancelada'}</span>
        ${r.estado==='confirmada'?`<button class="btn-cancel" onclick="openCancelModal(${r.id})">Cancelar</button>`:''}
      </div>
    </div>`).join('');
}

let pendingCancelId=null;
function openCancelModal(id){ pendingCancelId=id; document.getElementById('cancelModal').classList.add('open'); }
function closeCancelModal(){ pendingCancelId=null; document.getElementById('cancelModal').classList.remove('open'); }
function confirmCancel(){
  const r = reservas.find(x=>x.id===pendingCancelId);
  if(r){
    r.estado='cancelada';
    save();
    closeCancelModal();
    renderMisClases();
    showNotif('warning','Clase Cancelada',`Cancelación de ${r.curso} del ${r.fecha} procesada.`);
    setTimeout(()=>showNotif('warning','Notificación enviada',`Se notificó a ${r.nombre} (${r.email}).`),1400);
  }
}

// =====================================================
// NOTIFICACIONES
// =====================================================
function showNotif(type,title,msg){
  const c=document.getElementById('notifContainer');
  const el=document.createElement('div');
  el.className=`notif ${type}`;
  el.innerHTML=`<div class="notif-icon">${type==='success'?'✓':'✕'}</div>
    <div class="notif-msg"><div class="notif-title">${title}</div>${msg}</div>
    <button class="notif-close" onclick="closeNotif(this)">×</button>`;
  c.appendChild(el);
  setTimeout(()=>closeNotif(el.querySelector('.notif-close')),5500);
}
function closeNotif(btn){
  const n=btn.closest('.notif');
  n.classList.add('closing');
  setTimeout(()=>n.remove(),300);
}
function closeModal(id){ document.getElementById(id).classList.remove('open'); }

// =====================================================
// ADMIN — LOGIN
// =====================================================
function checkAdmin(){
  if(document.getElementById('adminPass').value==='admin123'){
    document.getElementById('adminLock').style.display='none';
    document.getElementById('adminDash').style.display='block';
    adminTab('resumen', document.querySelector('.tab'));
  } else {
    showNotif('warning','Contraseña incorrecta','Intenta nuevamente.');
  }
}

function adminTab(tab, btn){
  document.querySelectorAll('.tab').forEach(b=>b.classList.remove('active'));
  btn.classList.add('active');
  ['Resumen','Profesores','Cursos','Reservas'].forEach(t=>{
    document.getElementById('tab'+t).style.display = (tab===t.toLowerCase())?'block':'none';
  });
  if(tab==='resumen')    updateResumen();
  if(tab==='profesores') renderAdminProfesores();
  if(tab==='cursos')     renderAdminCursos();
  if(tab==='reservas')   renderAdminReservas();
}

// =====================================================
// ADMIN — RESUMEN FINANCIERO
// =====================================================
function updateResumen(){
  const conf = reservas.filter(r=>r.estado==='confirmada');
  const ing  = conf.reduce((s,r)=>s+r.precio,0);
  const cst  = 780000+(conf.length*4000);
  const util = ing-cst;
  const mg   = ing>0?Math.round(util/ing*100):0;
  document.getElementById('metIngresos').textContent = fmt(ing);
  document.getElementById('metIngSub').textContent   = `${conf.length} clases pagadas`;
  document.getElementById('metCostos').textContent   = fmt(cst);
  document.getElementById('metUtilidad').textContent = fmt(util);
  document.getElementById('metUtilidad').className   = 'metric-value '+(util>=0?'green':'red');
  document.getElementById('metUtiSub').textContent   = `margen ${mg}%`;
  document.getElementById('metReservas').textContent = conf.length;
  renderBarChart(ing, cst);
}
function renderBarChart(ingAct, cstAct){
  const meses=['Dic','Ene','Feb','Mar','Abr','May'];
  const bi=[420000,510000,480000,620000,590000,ingAct];
  const bc=[380000,390000,400000,410000,415000,cstAct];
  const mx=Math.max(...bi,...bc)||1;
  document.getElementById('barChart').innerHTML=meses.map((m,i)=>`
    <div class="bar-group">
      <div class="bar-wrap">
        <div class="bar income" style="height:${Math.round(bi[i]/mx*120)}px"></div>
        <div class="bar cost"   style="height:${Math.round(bc[i]/mx*120)}px"></div>
      </div>
      <div class="bar-label">${m}</div>
    </div>`).join('');
}
function simularCancelacionAdmin(){
  const act=reservas.filter(r=>r.estado==='confirmada');
  if(!act.length){showNotif('warning','Sin clases activas','No hay reservas confirmadas.');return;}
  act[0].estado='cancelada'; save(); updateResumen();
  showNotif('warning','Cancelación simulada',`Clase de ${act[0].curso} cancelada. Notificando a ${act[0].nombre}...`);
  setTimeout(()=>showNotif('warning','Notificación enviada',`${act[0].nombre} fue notificado/a por correo.`),1400);
}

// =====================================================
// ADMIN — PROFESORES
// =====================================================
let editProfesorId=null;

function renderAdminProfesores(){
  const el=document.getElementById('adminProfesoresList');
  el.innerHTML = profesores.map(p=>`
    <div class="profesor-admin-card">
      <div class="card-top">
        <div style="display:flex;align-items:center;gap:12px">
          <div class="profesor-avatar" style="width:48px;height:48px;font-size:1.2rem;margin:0">${p.inicial}</div>
          <div>
            <div class="profesor-name" style="font-size:0.95rem">${p.nombre}</div>
            <div class="profesor-spec" style="font-size:0.62rem">${p.spec}</div>
            <div style="margin-top:4px">
              <span class="clase-status ${p.activo?'status-confirmada':'status-cancelada'}">${p.activo?'Activo':'Inactivo'}</span>
            </div>
          </div>
        </div>
        <div style="display:flex;gap:6px">
          <button class="btn-icon" onclick="abrirModalProfesor(${p.id})" title="Editar">✎</button>
          <button class="btn-icon danger" onclick="eliminarProfesor(${p.id})" title="Eliminar">✕</button>
        </div>
      </div>
      <div style="font-size:0.7rem;color:var(--light-gray)">
        <strong style="color:var(--gold)">Cursos:</strong> ${p.cursos.join(', ')||'—'}
      </div>
      <div style="font-size:0.7rem;color:var(--light-gray);margin-top:4px">
        <strong style="color:var(--gold)">Horarios:</strong> ${(p.horarios||[]).join(' · ')||'Sin horarios'}
      </div>
    </div>`).join('');
}

function abrirModalProfesor(id){
  editProfesorId = id;
  const p = id ? profesores.find(x=>x.id===id) : null;
  document.getElementById('modalProfTitle').textContent = p ? 'Editar Instructor/a' : 'Nuevo/a Instructor/a';
  document.getElementById('mpNombre').value    = p ? p.nombre   : '';
  document.getElementById('mpSpec').value      = p ? p.spec     : '';
  document.getElementById('mpIniciales').value = p ? p.inicial  : '';
  document.getElementById('mpActivo').checked  = p ? p.activo   : true;

  // Checkboxes de cursos
  const cNames = cursos.map(c=>c.nombre);
  document.getElementById('mpCursosCheck').innerHTML = cNames.map(n=>`
    <label style="display:flex;align-items:center;gap:8px;margin-bottom:6px;font-size:0.78rem;cursor:pointer">
      <input type="checkbox" value="${n}" ${p&&p.cursos.includes(n)?'checked':''}
        style="accent-color:var(--gold);width:15px;height:15px"/> ${n}
    </label>`).join('');

  // Checkboxes de horarios
  document.getElementById('mpHorariosCheck').innerHTML = `
    <div style="display:flex;flex-wrap:wrap;gap:6px">
    ${TODOS_HORARIOS.map(h=>`
      <label style="display:flex;align-items:center;gap:5px;font-size:0.75rem;cursor:pointer;
        background:rgba(201,169,110,0.06);border:1px solid rgba(201,169,110,0.15);border-radius:3px;padding:5px 10px">
        <input type="checkbox" value="${h}" ${p&&(p.horarios||[]).includes(h)?'checked':''}
          style="accent-color:var(--gold)"/> ${h}
      </label>`).join('')}
    </div>`;

  document.getElementById('modalProfesor').classList.add('open');
}

function guardarProfesor(){
  const nombre    = document.getElementById('mpNombre').value.trim();
  const spec      = document.getElementById('mpSpec').value.trim();
  const inicial   = document.getElementById('mpIniciales').value.trim().toUpperCase();
  const activo    = document.getElementById('mpActivo').checked;
  const cursosSel = [...document.querySelectorAll('#mpCursosCheck input:checked')].map(i=>i.value);
  const horSel    = [...document.querySelectorAll('#mpHorariosCheck input:checked')].map(i=>i.value).sort();

  if(!nombre){showNotif('warning','Nombre requerido','Ingresa el nombre del instructor/a.');return;}

  if(editProfesorId){
    const p=profesores.find(x=>x.id===editProfesorId);
    p.nombre=nombre; p.spec=spec; p.inicial=inicial||p.inicial;
    p.activo=activo; p.cursos=cursosSel; p.horarios=horSel;
  } else {
    profesores.push({id:nextId(profesores),nombre,spec,inicial:inicial||nombre.substring(0,2).toUpperCase(),activo,cursos:cursosSel,horarios:horSel});
  }
  save();
  closeModal('modalProfesor');
  renderAdminProfesores();
  showNotif('success','Guardado','Instructor/a actualizado correctamente.');
}

function eliminarProfesor(id){
  if(!confirm('¿Eliminar este instructor/a?')) return;
  profesores = profesores.filter(p=>p.id!==id);
  save(); renderAdminProfesores();
  showNotif('warning','Eliminado','Instructor/a eliminado/a.');
}

// =====================================================
// ADMIN — CURSOS
// =====================================================
let editCursoId=null;

function renderAdminCursos(){
  const el=document.getElementById('adminCursosList');
  el.innerHTML = cursos.map(c=>`
    <div class="admin-section" style="padding:1.2rem 1.5rem">
      <div style="display:flex;align-items:center;justify-content:space-between">
        <div>
          <div style="font-family:var(--font-display);font-size:1.05rem;margin-bottom:4px">${c.nombre}
            <span style="font-size:0.6rem;padding:2px 8px;background:rgba(201,169,110,0.1);color:var(--gold);border-radius:2px;letter-spacing:0.08em;vertical-align:middle;margin-left:6px">${c.nivel}</span>
          </div>
          <div style="font-size:0.72rem;color:var(--light-gray)">
            Precio: <strong style="color:var(--gold)">${fmt(c.precio)}</strong>
            &nbsp;·&nbsp; Costo: <strong style="color:#D98080">${fmt(c.costo)}</strong>
            &nbsp;·&nbsp; Margen: <strong class="${c.precio-c.costo>=0?'td-pos':'td-neg'}">${fmt(c.precio-c.costo)}</strong>
          </div>
        </div>
        <div style="display:flex;gap:6px">
          <button class="btn-icon" onclick="abrirModalCurso(${c.id})">✎</button>
          <button class="btn-icon danger" onclick="eliminarCurso(${c.id})">✕</button>
        </div>
      </div>
    </div>`).join('');
}

function abrirModalCurso(id){
  editCursoId=id;
  const c=id?cursos.find(x=>x.id===id):null;
  document.getElementById('modalCursoTitle').textContent = c?'Editar Curso':'Nuevo Curso';
  document.getElementById('mcNombre').value = c?c.nombre:'';
  document.getElementById('mcNivel').value  = c?c.nivel:'Principiante';
  document.getElementById('mcPrecio').value = c?c.precio:'';
  document.getElementById('mcCosto').value  = c?c.costo:'';
  document.getElementById('modalCurso').classList.add('open');
}

function guardarCurso(){
  const nombre=document.getElementById('mcNombre').value.trim();
  const nivel =document.getElementById('mcNivel').value;
  const precio=parseInt(document.getElementById('mcPrecio').value)||0;
  const costo =parseInt(document.getElementById('mcCosto').value)||0;
  if(!nombre){showNotif('warning','Nombre requerido','Ingresa el nombre del curso.');return;}
  if(precio<=0){showNotif('warning','Precio inválido','Ingresa un precio válido.');return;}
  if(editCursoId){
    const c=cursos.find(x=>x.id===editCursoId);
    c.nombre=nombre; c.nivel=nivel; c.precio=precio; c.costo=costo;
  } else {
    cursos.push({id:nextId(cursos),nombre,nivel,precio,costo});
  }
  save(); closeModal('modalCurso'); renderAdminCursos();
  showNotif('success','Guardado','Curso actualizado correctamente.');
}

function eliminarCurso(id){
  if(!confirm('¿Eliminar este curso?')) return;
  cursos=cursos.filter(c=>c.id!==id);
  save(); renderAdminCursos();
  showNotif('warning','Eliminado','Curso eliminado.');
}

// =====================================================
// ADMIN — RESERVAS
// =====================================================
function renderAdminReservas(){
  const tb=document.getElementById('reservasTableBody');
  if(!reservas.length){
    tb.innerHTML='<tr><td colspan="7" style="text-align:center;color:var(--gray);padding:2rem">Sin reservas aún</td></tr>';
    return;
  }
  tb.innerHTML=reservas.slice(0,50).map(r=>`
    <tr>
      <td>${r.nombre||'—'}</td>
      <td>${r.curso}</td>
      <td>${r.fecha} ${r.hora}</td>
      <td>${r.profesora?r.profesora.split(' ')[0]:'—'}</td>
      <td><span class="clase-status status-${r.estado}">${r.estado}</span></td>
      <td style="color:var(--gold)">${fmt(r.precio)}</td>
      <td>${r.estado==='confirmada'?`<button class="btn-icon danger" onclick="cancelarReservaAdmin(${r.id})" title="Cancelar">✕</button>`:'—'}</td>
    </tr>`).join('');
}

function cancelarReservaAdmin(id){
  const r=reservas.find(x=>x.id===id);
  if(!r||!confirm(`¿Cancelar la reserva de ${r.nombre}?`)) return;
  r.estado='cancelada'; save(); renderAdminReservas(); updateResumen();
  showNotif('warning','Reserva cancelada',`Se canceló la clase de ${r.nombre}. Notificación enviada.`);
}

// =====================================================
// INIT
// =====================================================
renderCalendar();
</script>
</body>
</html>
